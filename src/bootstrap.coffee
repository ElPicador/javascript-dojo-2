$ ->
	plateau = new Plateau(Raphael(10, 50, 320, 200))
	plateau.draw()
	# paper = Raphael(10, 50, 320, 200)
	# rectangle = paper.rect(0, 0, 320,200);
	# rectangle.attr("stroke", "black");

	# laCase = paper.rect(10,10,32,20);
	# laCase.attr("fill","black");
	# laCase.click ()->
	# 	laCase.attr("fill", "red")